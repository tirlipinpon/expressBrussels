import { NotDesactivePipe } from './not-desactive.pipe';

describe('NotDesactivePipe', () => {
  it('create an instance', () => {
    const pipe = new NotDesactivePipe();
    expect(pipe).toBeTruthy();
  });
});

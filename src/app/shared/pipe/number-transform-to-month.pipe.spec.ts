import { NumberTransformToMonthPipe } from './number-transform-to-month.pipe';

describe('NumberTransformToMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberTransformToMonthPipe();
    expect(pipe).toBeTruthy();
  });
});

import { ValidatorClassDirective } from './validator-class.directive';

describe('ValidatorClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorClassDirective();
    expect(directive).toBeTruthy();
  });
});

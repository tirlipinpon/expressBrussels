import { OnBlurDirective } from './on-blur.directive';

describe('OnBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new OnBlurDirective();
    expect(directive).toBeTruthy();
  });
});
